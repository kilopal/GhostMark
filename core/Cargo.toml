[package]
name = "ghostmark-core"
version.workspace = true
edition.workspace = true
description = "Core memory-safe engine for stripping AI watermarks"

[dependencies]
img-parts = "0.3.0"
bytes = "1.5.0"
